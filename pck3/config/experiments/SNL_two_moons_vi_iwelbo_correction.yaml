# @package _global_

defaults:
  - override /task: two_moons
  - override /method: snlvi

task:
  vi_parameters:
    loss: iwelbo
    n_particles: 32
    K: 8
    reduce_variance: False
    max_num_iters: 1000
    min_num_iters: 100
    bound: 15
    count_bins: 15
    sampling_method: ir

method:
  params:
    num_rounds: 10