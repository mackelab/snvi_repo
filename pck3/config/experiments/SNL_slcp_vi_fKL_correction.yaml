# @package _global_

defaults:
  - override /task: slcp
  - override /method: snlvi

task:
  vi_parameters:
    loss: forward_kl
    n_particles: 256
    proposal: "q"
    max_num_iters: 1000
    min_num_iters: 100
    bound: 15
    count_bins: 15
    sampling_method: ir



method:
  params:
    num_rounds: 10